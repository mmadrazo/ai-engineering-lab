info:
  name: Test Inicial
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8000/api/v1/estimate
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
